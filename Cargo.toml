[package]
name = "algoritmo"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.4", features = ["derive"] }

rand = "0.8"
rand_chacha = "0.3"

stats_alloc = "0.1.1"

[target.'cfg(target_os = "linux")'.dependencies]
perf-event = "0.4"